sudo: required
dist: trusty
language: cpp

services:
  - docker

branches:
  only:
    - master
    - develop

before_install:
  - docker build -t travis-build -f tools/Dockerfile-travis .

script:
  # Common style check
  - docker run travis-build tools/checkstyle.sh $TRAVIS_COMMIT_RANGE
  # Run clang analysis tools
  - docker run travis-build /bin/bash -c "mkdir build-clang-tidy && cd build-clang-tidy && cmake .. && make clang-tidy"
  # Run IWYU
  - docker run travis-build /bin/bash -c "cd build-clang-debug && make iwyu"
  # Clang, build debug and release and run tests for debug build
  - docker run travis-build /bin/bash -c "cd build-clang-debug && make && ctest --output-on-failure"
  - docker run travis-build /bin/bash -c "cd build-clang-release && make && ctest --output-on-failure"
  # GCC, build debug and release and run tests for debug
  - docker run travis-build /bin/bash -c "cd build-gcc-debug && make && ctest --output-on-failure"
  - docker run travis-build /bin/bash -c "cd build-gcc-release && make && ctest --output-on-failure"
